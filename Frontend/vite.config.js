import { defineConfig } from 'vite'

export default defineConfig({
  // Jika Anda akan deploy ke sub-path (misal: namadomain.com/app/),
  // Anda mungkin perlu menambahkan base: '/app/' di sini.
  // Untuk saat ini, biarkan kosong.
})